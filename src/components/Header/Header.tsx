

export default function Header(){
    return (
    <div
      style={{
        height: 60,
        background: "#111",
        color: "#6C76BB",
        display: "flex",
        alignItems: "center",
        padding: "0 20px",
        fontSize: 20,
        fontWeight: "bold"
      }}
    >
      WDSE
    </div>
  );
}